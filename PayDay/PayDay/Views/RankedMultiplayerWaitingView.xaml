<UserControl x:Class="PayDay.Views.RankedMultiplayerWaitingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PayDay.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightBlue">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock FontSize="30">Spieler werden gesucht....</TextBlock>
            <TextBlock HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="20" Text="{Binding Path=Timertext,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">0</TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
